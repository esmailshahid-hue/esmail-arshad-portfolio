---
type: tool
title: Supply Chain Workbench
organization: Independent Project
roleFocus: Supply Chain Analytics
timeframe: '2026'
summary: Built three supply-chain workflows for inventory planning, freight exceptions and supplier performance, with AI-generated action summaries.
capabilities:
  - Inventory Planning
  - Freight Analytics
  - Supplier Performance
tools:
  - JavaScript
  - CSV
  - DeepSeek
links:
  live: https://esmail-supply-chain-ai-toolkit-blond.vercel.app/
  github: https://github.com/esmailshahid-hue/supply-chain-ai-toolkit
atAGlance:
  - label: Workflows
    value: '3'
    note: inventory · freight · supplier
  - label: Data input
    value: Flexible CSV
    note: automatic + manual column mapping
  - label: Processing
    value: Local
    note: calculations run in-browser · no database
rightsStatus: own-work
images:
  - file: supply-chain-ai-toolkit.png
    sheet: Inventory Planner
    alt: Supply Chain Workbench showing the Inventory Planner with summary KPIs, inventory status results and navigation to Freight Exceptions and Supplier Scorecard.
    caption: Live toolkit view; Freight Exceptions and Supplier Scorecard use the same interface.
    primary: true
featured: false
published: true
draft: false
order: 8
---

## What I built

I built three reusable workflows for inventory planning, freight exception analysis and
supplier performance. Upload a CSV, map fields where needed, then filter, sort and export the
calculated results.

## Three workflows

### Inventory Planner

Calculates days of cover, lead-time demand, reorder points, inventory position and
MOQ-rounded order recommendations.

### Freight Exceptions

Benchmarks cost per mile against the lane average, then flags high-cost or late shipments. A
shipment meeting both conditions is marked Critical.

### Supplier Scorecard

Scores suppliers across delivery, quality, cost and service, with configurable weights.
Suppliers missing required data are marked Incomplete rather than assigned a score the data
does not support.

## How it works

Upload CSV → map columns → analyze → filter/sort → export results

You do not need exact header names. Common variants of each field are matched automatically,
so a file that calls a column `Stock On Hand`, `on_hand` or `Quantity On Hand` resolves the
same way. If a required field is left unresolved, the mapping interface lets you pick the right
column before the analysis runs. Columns the workflow does not use are ignored.

## Build approach

- **Development workflow.** I used Claude and Codex for implementation, iteration and QA,
  while defining the supply-chain rules and expected outputs explicitly and validating them
  against known results.
- **Deterministic analysis.** Inventory, freight and supplier calculations are defined in code
  rather than generated by a language model.
- **Browser-side processing.** Uploaded CSV data is parsed and analyzed in the browser. No
  database is required and session data is not persisted.
- **Action Summary.** DeepSeek generates a short summary from already-calculated structured
  results when requested. Raw uploaded CSV files are not sent to the AI endpoint.
